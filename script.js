//original
//cipher

function encryption() {
    const char = 'ABC';
    const ascii = '';
    for (let i = 0; i < char.length; i++) {
        const ascii = char.charCodeAt(i);
    }  
    console.log(ascii); // 出力: 65
}
