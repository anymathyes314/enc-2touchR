//original
//cipher

function encryption{
  ver nowcode = "";
  ver text = document.getElementByid(original);
  ver len = original.length;
  ver len = len - 1;
  for ( ver i = 0; i < len; i++ ) {
    text.charCodeAt(i);
  }
}
