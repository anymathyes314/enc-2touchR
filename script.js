//original
//cipher

function encryption() {
  const str = 'ABC';
  const values = [];

  for (let i = 0; i < byteArray.length; i++) {
    const values = values + str.charCodeAt(i);
  }

  console.log(values);
}
